

## Bump da versão do Agent

Atualizar `python-agent/agent/version.py` de `1.3.12` para `1.3.13` para refletir a correção do fallback de disco no `http_request.py`.

### Arquivo
- `python-agent/agent/version.py`: `__version__` de `"1.3.12"` → `"1.3.13"`

