

# Deploy da versao 1.2.8 do Agent

## Alteracao

| Arquivo | Mudanca |
|---|---|
| `python-agent/agent/version.py` | Alterar `__version__` de `"1.2.7"` para `"1.2.8"` |

Isso marca a nova versao que inclui as correcoes de `AmbientCapabilities`, `setcap` no `check-deps.sh`, e os executors corrigidos.

